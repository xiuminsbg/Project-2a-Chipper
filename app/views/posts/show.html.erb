<p id="notice"><%= notice %></p>


<% @posts.each do |f| %>
  <div class="cell">
    <h4><%= f.title %></h4>
    <p><%= f.body %></p>
  </div>
<% end %>
<%= link_to 'Back', user_posts_path(current_user) %>
